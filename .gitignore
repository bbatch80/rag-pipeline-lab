# Never commit — local planning material
plans/
CLAUDE.md
.DS_Store

# Secrets — the only file that ever holds real credentials
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Local data artifacts
data/

# Airflow runtime home (DAG files are committed in dags/)
.airflow/
